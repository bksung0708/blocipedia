<div class="row">
  <div class="col-md-8">
    <h1><%= markdown(@wiki.title) %></h1>
    <p class="lead"><%= markdown(@wiki.body) %></p>
  </div>
   <div class="col-md-2">
     <br><%= link_to "Delete Wiki", @wiki, method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this topic?' } %>
     <br><br><%= link_to "Edit Wiki", edit_wiki_path, class: 'btn btn-success' %>
   </div>
</div>
